{
  "name": "relaymark.oauth2",
  "version": "1.0.23",
  "description": "Relaymark service to use OAuth2 authentification in authorization code flow.",
  "main": "./dist/relaymark.oauth2.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && webpack --debug --progress --display-error-details",
    "clean": "rimraf dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Relaymark/Relaymark.oauth2.git"
  },
  "keywords": [
    "relaymark",
    "oauth2",
    "authorization",
    "authentification"
  ],
  "author": "Relaymark",
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-core": "^6.7.7",
    "babel-loader": "^6.2.4",
    "babel-plugin-ng-annotate": "^0.3.1",
    "babel-plugin-syntax-decorators": "^6.5.0",
    "babel-preset-angular": "^6.0.15",
    "babel-preset-es2015": "^6.6.0",
    "html-loader": "^0.4.3",
    "ngtemplate-loader": "^1.3.1",
    "rimraf": "^2.5.2",
    "webpack": "^1.13.0"
  },
  "license": "ISC",
  "dependencies": {
    "angular": "^1.5.2",
    "ngstorage": "^0.3.10"
  }
}
